<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.health">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".list10Details"></activity>
        <activity android:name=".list9Details" />
        <activity android:name=".list8Details" />
        <activity android:name=".list7Details" />
        <activity android:name=".list6Details" />
        <activity android:name=".list5Details" />
        <activity android:name=".list4Details" />
        <activity android:name=".list3Details" />
        <activity android:name=".list2Details" />
        <activity android:name=".list1Details" />
        <activity android:name=".list" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>